# Reproducibility Notes

Run from the article folder:

```bash
./run_all.sh
```

Generated outputs are written to:

```text
outputs/tables/
outputs/figures/
```

Python uses only the standard library. R uses base R.

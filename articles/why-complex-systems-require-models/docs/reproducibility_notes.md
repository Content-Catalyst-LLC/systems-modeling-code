# Reproducibility Notes

Run from the article folder:

```bash
./run_all.sh
```

Generated outputs are written to:

```text
outputs/tables/
outputs/figures/
```

The Python workflow uses only the standard library. The R workflow uses base R. Compiled-language workflows run only when the corresponding compilers are installed.
